int main (){
int arr[5]={10,11,12,13,14};
int low=0,high=4,mid,item=13;
while(low<=high){
mid=(low+high)/2;

 if(arr[mid}==item){
 print("element found at position %d",mid+1);
 return 0;

 else if (arr[mid]<item)
 low=mid+1;
 else
 high=mid-1;
 }
 }
 printf("element not found");
 return 0;
 }


