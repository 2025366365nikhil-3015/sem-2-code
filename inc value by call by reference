#include <stdio.h>

void increment(int *num) {
    (*num)++;   // pointer dereferencing
}

int main() {
    int x;

    printf("Enter a number: ");
    scanf("%d", &x);

    increment(&x);

    printf("After increment: %d", x);

    return 0;
}
